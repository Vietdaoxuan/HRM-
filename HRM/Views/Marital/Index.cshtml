@model HRM.Models.LSMaritalModel

@{
    ViewBag.Title = "Index";
}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.css">

<h2 id="title"></h2>

<ul class="nav nav-pills" id="myTab" role="tablist">
    <li class="nav-item">
        <a class="nav-link active" data-toggle="tab" id="Tlist-tab" href="#list">List</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" onclick="AddMarital(0)" id="Infor-tab" data-toggle="tab" href="#Infor"></a>
    </li>
</ul>

<div class="tab-content" id="myTabContent">
    <div class="tab-pane fade in active" id="list" style="margin-top:35px">
        <table class="display" id="listMarital">
            <thead>
                <tr>
                    <th>LSMaritalID</th>
                    <th>LSMaritalCode</th>
                    <th>Name</th>
                    <th>VNName</th>
                </tr>
            </thead>

        </table>
    </div>

    <div id="Infor" class="tab-pane fade" style="position:absolute;top:12em">
        <form id="form">
            <fieldset id="Submitform">

                <input type="hidden" id="LSMaritalID" name="LSMaritalID">

                <div class="form-group col-md-10">
                    <label for="LSMaritalCode" class="control-label col-md-2">Marital Code</label>
                    <input type="text" class="form-control" id="LSMaritalCode" name="LSMaritalCode" />
                </div>

                <div class="form-group col-md-10">
                    <label for="Name" class="control-label col-md-2">Name</label>
                    <input type="text" class="form-control" id="Name" name="Name" />
                </div>

                <div class="form-group col-md-10">
                    <label for="Rank" class="control-label col-md-2">Rank</label>
                    <input type="text" class="form-control" id="Rank" name="Rank" />
                </div>

                <div class="form-group col-md-10">
                    @Html.LabelFor(model => model.Used, htmlAttributes: new { @class = "control-label col-md-2" })
                    @Html.EditorFor(model => model.Used, new { htmlAttributes = new { @class = "form-control" } })
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input value="Save" class="btn btn-default" id="SaveMarital" />
                    </div>
                </div>
            </fieldset>
        </form>
    </div>
</div>
<link href="//cdn.datatables.net/1.10.20/css/jquery.dataTables.min.css" rel="stylesheet" />
@section scripts{
    <script src="~/Scripts/jquery-3.3.1.min.js"></script>

    <script src="//cdn.datatables.net/1.10.20/js/jquery.dataTables.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.js"></script>
    <script>
        $(document).ready(function (res) {
            document.getElementById("title").innerHTML = "Index";
            document.getElementById("Infor-tab").innerHTML = "Create";
            $('#listMarital').DataTable({
                ajax: {
                    //url: "/api/MaritalAPI/GetData",
                    url: "Marital/GetListApi",
                    type: "Get",
                    //datatype: "json",
                    dataSrc: ""
                },

                "columns": [
                    {
                        "targets": [0],
                        "data": "LSMaritalID"
                    },
                    {
                        "targets": [1],
                        "data": "LSMaritalCode"
                    },
                    {
                        "targets": [2],
                        "data": "Name"
                    },
                    {
                        "targets": [4],
                        "data": "Used"
                    },
                    {
                        "targets": [5],
                        "data": "LSMaritalID",
                        "render": function (data, type, row, meta) {
                            return "<a class = 'btn btn-warning nav-link' href = '#Infor' data-toggle='tab' onclick = 'EditMarital(\"" + data + "\")'>Edit</a>" +
                                   " <a class = 'btn btn-danger' onclick = 'DeleteMarital(\"" + data + "\")'>Delete</a>";
                        }
                    },
                ],
                "serverside": "true",
                'order': [0, "asc"],
            });
            $("#listMarital_filter input").prop('id', 'search_box');
            $("#search_box").css({
                "borderRadius": "11px", "height": "40px", "border": "1px solid gainsboro"
            });
            $("#search_box").focus(function () {
                $("#search_box").css({ "outline": "none" });
            })
        })

        $('#listMarital thead tr').append('<th/>');

        $("#Tlist-tab").click(function () {
            document.getElementById("title").innerHTML = "Index";
            document.getElementById("Infor-tab").innerHTML = "Create";
            document.getElementById("list").style.height = "0em";
        })

        function AddMarital(LSMaritalID) {
            document.getElementById("myTabContent").style.marginBottom = '27em';
            $("#LSMaritalID").val("");
            $("#LSMaritalCode").val("");
            $("#Name").val("");
            $("#Rank").val("");
            document.getElementById("title").innerHTML = "Create New";
            document.getElementById("Infor-tab").innerHTML = "Create";
            document.getElementById("list").style.height = '26em';
            $("#SaveMarital").click(function () {
                var data = $("#Submitform").serialize();
                $.ajax({
                    type: "Post",
                    url: "/api/MaritalAPI/Create",
                    data: data,
                    success: function (response) {
                        alert("Success!");
                        window.location.href = "/Marital/Index";
                    }
                })
            })
        }

        function EditMarital(ID) {
            console.log(ID);
            document.getElementById("title").innerHTML = "Edit Record";
            document.getElementById("Infor-tab").innerHTML = "Edit";
            document.getElementById("myTabContent").style.marginBottom = "26em";
            $("#Infor-tab").tab('show');
            $.ajax({
                type: 'GET',
                url: '/api/MaritalAPI/GetDataByID/' + ID,
                dataType: 'json',
                success: function (data) {
                    console.log(data);
                    for (var i = 0; i < data.len
                        gth; i++) {
                        if (data[i].LSMaritalID === ID) {
                            $("#LSMaritalID").val(data[i].LSMaritalID);
                            $("#LSMaritalCode").val(data[i].LSMaritalCode);
                            $("#Name").val(data[i].Name);
                            $("#Rank").val(data[i].Rank);
                            break;
                        }
                    }
                }
            });
            $("#SaveMarital").click(function () {
                console.log(LSMaritalID);
                var data = $("#Submitform").serialize();
                $.ajax({
                    type: "Put",
                    url: '/api/MaritalAPI/Update/' + ID,
                    data: data,
                    success: function (response) {
                        alert("success!")
                        window.location.href = "/Marital/Index";
                    }
                })
            })
        }

        function DeleteMarital(LSMaritalID) {
            console.log(LSMaritalID);
            swal({
                title: "Are you sure?",
                text: "You will not be able to recover this record!",
                type: "warning",
                showCancelButton: true,
                confirmButtonColor: "#DD6B55",
                confirmButtonText: "Yes, Delete it!",
                cancelButtonText: "No, cancel plx!",
                closeOnConfirm: false,
                closeOnCancel: false
            },
            function (isConfirm) {
                if (isConfirm) {
                    $.ajax({
                        type: 'Post',
                        url: '/api/MaritalAPI/Delete/' + LSMaritalID,
                        success: function (response) {
                            window.location.href = "/Marital/Index";
                        },
                        error: function (data) {
                            swal("NOT Deleted!", "Something went wrong.", "error");
                        }
                    });
                } else {
                    swal("Cancelled", "Your record is safe :)", "error");
                }
            });
        return false;
        }
    </script>
}

